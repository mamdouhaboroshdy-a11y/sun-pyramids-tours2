rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Catch-all default allow for development database and unrestricted admin access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
